# 工單 #v3-001:建立使用者級 AI 協作層(零覆寫)

> 版本:零覆寫版 | 老師:Opus(網頁版) | 工程師:Sonnet(Claude Code CLI)
> 依據:RELIABLE_WORKFLOW v3 / 記憶 #29

## 背景
甲方要把三角分工(甲方/老師=Opus orchestrator/工程師=Sonnet worker)從跨工具手動
複製貼上,收斂進單一 Claude Code 環境,讓 orchestrator 原生觀測 worker 執行。
架構:兩層分離。使用者級 ~/.claude/ = 通用協作層,所有專案繼承;專案級
各專案/.claude/CLAUDE.md = 專屬脈絡。本工單只做使用者級。專案級既有檔(尤其
InvestSys 24KB)一律不碰,留 #v3-002。

## 最高優先約束(違反即作廢)
1. 絕對不得修改/覆蓋/刪除任何專案級 .claude/CLAUDE.md(特別是 InvestSys 24KB)。
   所有寫入只發生在 ~/.claude/。動工前確認每個寫入路徑都以 ~/.claude/ 開頭。
2. 不得自動生成/掃描 codebase 來「補充」CLAUDE.md(ETH Zurich 2026:AI 生成 context
   降表現)。內容由老師逐字指定,你只負責寫進檔案,不擴寫。
3. 若 ~/.claude/CLAUDE.md 已存在:不覆蓋,改在最前面插入本框架區段,既有內容保留,
   並在計畫書回報既有內容摘要供老師裁決。

## 設計原則
1. Claude Code 只讀 CLAUDE.md 不讀 AGENTS.md。
2. subagent 是 .md + frontmatter(name/description/tools)+ body(system prompt),非純 YAML。
3. 維持「老師+一個 worker+甲方」三角,不建平行 fleet、不 fan-out。
4. 防作弊紀律(先 red 後 green、禁止 test 與實作同時生)寫死進 worker system prompt。

## 交付項目(全部在 ~/.claude/ 之下)
1. ~/.claude/CLAUDE.md(第 4 節)
2. ~/.claude/agents/engineer-tdd.md(第 5 節)
3. ~/.claude/agents/verifier.md(第 6 節)
4. ~/.claude/SETUP_NOTES.md(路徑、啟動驗證、worktree 註記)

## 第 4 節:~/.claude/CLAUDE.md 內容(逐字落地,勿擴寫)

```
# 通用 AI 協作層(所有專案繼承)
> 此檔為使用者級。專案專屬脈絡放各專案 .claude/CLAUDE.md,不要寫進這裡。

## 協作模式:RELIABLE_WORKFLOW v3 三角分工
- 甲方(人):提需求、做最終決策、保留逃生閥控制權
- 老師(orchestrator, Opus):grill-me 對齊、垂直切片劃分把關、驗收
- 工程師(worker, Sonnet):紅綠重構執行、commit、回報
- 老師在統一環境(Claude Code)中,實作一律 dispatch 給 engineer-tdd subagent;老師自己不寫實作碼。老師輸出限於:切片劃分、規格、批改、驗收、交接。
人工關卡集中在「垂直切片劃分」階段,不逐份批改計畫書。切片通過後實作放 AFK。
高風險改動可由甲方標記,拉回逐份批改(逃生閥)。

## 開發紀律
- 垂直切片(tracer bullet):每片切過全層(schema→邏輯→測試),薄但端到端可驗證。
  禁止水平切片(先全部 test 再全部 code)。
- TDD:一次一個行為。Red(寫失敗 test、確認失敗)→ Green(最小實作)→ Refactor。
- Behavior test 而非 implementation test:test 讀起來像規格,refactor 後仍存活。
- Tiny commits:每步保持可運行。

## Context 紀律
- context 壓在約 100k token 以下。
- clear 重啟勝過 compact(compact 留沉積物拖垮品質)。
- clear 前由 orchestrator 產出交接內容(當前切片/已驗證事實/下一步/雷區)。

## TDD 適用邊界
red-green-refactor 只在「可立即驗證的程式行為」這層有效(計算邏輯、訊號 gate 布林判斷)。
不適用於需要延遲回饋才能判斷對錯的領域(如投資 thesis 正確性)。
```

## 第 5 節:~/.claude/agents/engineer-tdd.md 內容(逐字落地)

```
---
name: engineer-tdd
description: 工程師 worker。以 red-green-refactor 一次一個垂直切片實作。當任務是「實作一個可測試的程式行為」時使用。
tools: Read, Write, Edit, Bash, Grep, Glob
---
你是 RELIABLE_WORKFLOW v3 的工程師(worker)。你只做一件事:以 TDD 實作垂直切片。

## 鐵律
1. 先寫測試,一次只寫一個行為的測試。禁止把整批測試一次寫完。
2. 寫完測試先跑、親眼確認它失敗(red),才能寫實作。
   若新測試一寫就通過,代表沒在驗證新行為,停下回報異常。
3. 寫剛好讓該測試通過的最小實作(green)。不得順手實作測試沒要求的功能。
4. green 之後才可 refactor,全程保持綠燈。
5. 嚴禁同時生成實作與其測試。實作與測試在同一步寫出=被禁止的作弊路徑。
6. 每個垂直切片完成後 commit,訊息標明該切片行為。

## 回報
每切片回報:測試名稱、red 證據、green 證據、commit hash。
遇到無法在「可立即驗證」範圍內測試的需求(如涉及投資判斷對錯),停下標記,交回老師,
不要硬寫假測試蒙混。
```

## 第 6 節:~/.claude/agents/verifier.md 內容(逐字落地)

```
---
name: verifier
description: 獨立驗收 worker。檢查測試是否真正對應宣稱的行為,而非只看綠燈。當一個切片宣稱完成、需要獨立查核時使用。
tools: Read, Bash, Grep, Glob
---
你是獨立驗收者。職責不是「確認測試有沒有通過」,而是「確認測試在驗證真實行為」。
你不寫實作、不改測試,只查核並回報。
你的 Bash 僅允許唯讀命令(pytest --collect-only、cat、git log 等),不得執行任何寫入、改檔或刪除命令。

## 查核清單
1. 讀每個測試:它斷言的是「真實期望行為」還是「現有實作剛好的輸出」?
   警訊:期望值看起來像從實作回填(implementation test),而非從規格推導。
2. 試著破壞:假設實作有 bug,這測試抓得到嗎?抓不到=假測試。
3. 確認涵蓋邊界與失敗路徑,而非只有 happy path。
4. behavior 而非 implementation:實作 refactor 後測試會壞=脆弱測試,標記。

## 回報
逐測試給出:真實/可疑/假測試,附理由。發現假測試或只測 happy path 明確標出,交回老師。
不要因為「全綠」就放行。
```

## 第 7 節:工程師計畫書需回答的問題
1. 路徑確認:~/.claude/ 在這台 Mac mini M4 展開的絕對路徑?~/.claude/CLAUDE.md 是否
   已存在?若存在,既有內容摘要為何?
2. 零覆寫保證:列出本工單會寫入的每個絕對路徑,逐一確認沒有落在專案級 .claude/ 內。
3. tools 權限取捨:engineer-tdd 給 Write/Edit/Bash,verifier 只給唯讀。你同意嗎?
4. worktree 判斷:目前單 worker 不平行,需不需要配 git worktree?考量 InvestSys 在
   外接 4TB 碟、TCC 權限/launchd 存取已知問題。
5. 驗證任務:打算用哪個真實最小任務跑通三角?(建議:ta_quick_analysis.py 加純函式走
   完整 red-green-refactor)。注意:這步會動 InvestSys code,需老師另行批准,不在本工單
   「建配置」交付範圍內,只需在計畫書描述。

## 驗收標準
- [ ] 四檔建立在 ~/.claude/ 之下,路徑正確
- [ ] InvestSys 24KB 專案級 CLAUDE.md 一個字節都沒被改(git status/mtime 證明)
- [ ] ~/.claude/CLAUDE.md 內容與第 4 節一致,未自行膨脹
- [ ] engineer-tdd.md 明確寫死「先 red 再 green、禁止 test 與實作同時生」
- [ ] verifier.md 明確以「行為對應」而非「綠燈」為查核標準
- [ ] SETUP_NOTES.md 含啟動驗證步驟與 worktree 註記
- [ ] 計畫書五題全部回答,老師批改通過後才動工

## 不在範圍
- 不碰任何專案級 .claude/CLAUDE.md(含 InvestSys 24KB)→ #v3-002
- 不處理 Schwab 過時段落 → #v3-002
- 不做多 worker 平行 fleet、不 fan-out
- 不安裝 mattpocock/skills → 另案
- 不實際執行驗證任務的程式修改(只在計畫書描述)→ 需老師另行批准
- 不釐清 Hermes 與 Claude Code 三角分工 → 另案

# v3-004 — 把 dev-env 協作層搬出 InvestSys，獨立成 `dev-env-v3` repo

**開單日期**：2026-06-13
**類型**：跨 repo 搬遷（非 TDD，機械操作）
**執行者**：頂層 **Sonnet**（不走 engineer-tdd；純搬遷，勿用 Opus）
**狀態**：✅ 完成（2026-06-17 查核確認）。協作層已實體搬出 InvestSys、成為兄弟目錄 `dev-env-v3` 獨立 repo;InvestSys 端已解除追蹤(`git ls-files` 無 RELIABLE_WORKFLOW/v3-* 殘留)、`_archive/v3-001-draft.md` 跟著搬入、`ta.md` 正確留在 InvestSys、無壞 reference(BACKLOG.md 命中為流程名非路徑)。

---

## 背景

`RELIABLE_WORKFLOW.md` 與 `v3-*` 工單目前寄居在 InvestSys repo 內，但它們是**跨專案的開發協作層**，不屬於投資系統本身。本工單把這層搬出 InvestSys，落腳到新建的獨立 repo `dev-env-v3/`（已用本工單在 `dev-env-v3/tickets/` 開張，順手讓新家的 tickets 目錄成形）。

搬遷採「跨 repo git」：InvestSys 端 `git rm`（解除追蹤），`dev-env-v3` 端 `git add`（納入新 repo），檔案內容物理移入 `dev-env-v3/`。

---

## 目標

1. 在 `dev-env-v3/` 執行 `git init`，成為獨立 repo。
2. 把下列檔案從 InvestSys 搬入 `dev-env-v3/`：
   - `RELIABLE_WORKFLOW.md`
   - `.claude/tickets/v3-001.md`、`.claude/tickets/v3-003.md`（v3-004 本身已生在新家）
3. grep 全 InvestSys repo，找出**指向被搬走路徑**的 reference（任何檔提到 `RELIABLE_WORKFLOW.md` 或 tickets 路徑）。
4. 對兩個歸屬未定檔（`_archive/v3-001-draft.md`、`.claude/commands/ta.md`）給出「搬 / 留 / 刪」判斷。

---

## 硬安全閥（兩道 HITL 閘，禁止自動跑完）

### 閘 1：reference 不自己改

grep 出哪些檔指向被搬走的路徑後，**只列給甲方，不自動改**。
自動改 reference 容易連鎖弄壞（改錯一處、漏改一處都難回溯）。
補法由甲方逐項裁決：**指向新位置 / 改敘述 / 移除**。

### 閘 2：兩個歸屬未定檔不擅自決定

`_archive/v3-001-draft.md`、`.claude/commands/ta.md` 的歸屬，先**回報判斷與理由**，由甲方裁決，不自行搬 / 刪。

> 老師初判（供甲方參考，非定論）：
> - `commands/ta.md` 傾向**留**在 InvestSys——它是投資系統專屬的 TA slash command，非跨專案協作層。
> - `_archive/v3-001-draft.md` 是 v3-001 的草稿，與已搬走的 v3-001 同源，**傾向跟著搬**，但屬 `_archive`（廢稿），也可能直接刪。交甲方定。

---

## 計畫書先行（本回合唯一交付）

⚠️ **本回合只出計畫書，不動任何 git、不搬任何檔、不改任何 reference。**
計畫書四項，全部回報後等老師核可，**過了才放它動 git**：

1. **grep 結果**：grep InvestSys 全 repo，找出 reference 了 `RELIABLE_WORKFLOW.md` 或 tickets 路徑的檔，**貼字面輸出**（含檔名 + 命中行），不摘要、不改寫。
2. **git 指令序列**：跨 repo 搬遷的具體指令，**InvestSys 端 / `dev-env-v3` 端各下什麼**逐條列出（含 `git init`、檔案物理移動、`git rm`、`git add`、commit 訊息）。讓甲方能逐行核對再放行。
3. **兩檔判斷**：`_archive/v3-001-draft.md`、`.claude/commands/ta.md` 各給「搬 / 留 / 刪」+ 理由。
4. **`.gitignore` 內容**：提 `dev-env-v3/.gitignore` 該放什麼。

---

## 給執行者（Sonnet）的啟動須知

- 需同時碰得到 InvestSys 與 `dev-env-v3` 兩個目錄。啟動時用 `--add-dir` 或一律用絕對路徑。
- `dev-env-v3/` 是 InvestSys working tree 內的子目錄，`git init` 後成為**巢狀的獨立 repo**；注意別讓 InvestSys 把 `dev-env-v3/` 當一般檔案追蹤（`.gitignore` 或 `git rm --cached` 的取捨在計畫書 #2/#4 講清楚）。
- 搬遷要保內容：先物理移動檔案進 `dev-env-v3/`，InvestSys 端再解除追蹤；別用會丟內容的刪法。

---

## 完成定義（整張單，跨多回合）

1. 計畫書四項齊備，甲方核可（**本回合到此為止**）。
2. `dev-env-v3/` `git init` 完成，為獨立 repo。
3. `RELIABLE_WORKFLOW.md` + v3-001 / v3-003 內容完整移入 `dev-env-v3/`，InvestSys 端解除追蹤。
4. 兩檔歸屬依甲方裁決處置。
5. reference 依甲方逐項裁決補好（**由甲方拍板，非執行者自決**）。
6. `dev-env-v3/.gitignore` 落地。
7. 兩 repo 各自 `git status` 乾淨（除已知維持項）。

---

## 不在範圍

- 不自動改任何 reference（閘 1）。
- 不擅自決定兩檔歸屬（閘 2）。
- 不重寫 `RELIABLE_WORKFLOW.md` 或任何被搬檔的內容——只搬，不編輯。
- 不碰 InvestSys `.claude/CLAUDE.md`（24KB 專案記憶）與其他投資系統原始碼。
